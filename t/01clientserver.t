#!/usr/bin/perl

use warnings;
use strict;
use Test::More tests => 1;

BEGIN { use_ok('POE::Component::IKC::ReallySimple'); }

#ok(poemsg_receive( sub { is(shift,'TEST','poemsg_receive receives data') }));

#ok(poemsg_send('TEST'));

